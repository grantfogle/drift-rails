<header class="">
    <nav class="h-[60px] bg-white text-white border-gray-200 px-4 lg:px-6 py-2.5 dark:bg-blue-500 flex flex-row items-center justify-between">
        <div>
            <a href="#"><span>DRIFT</span></a>
        </div> 
        <div>
            <ul class="flex flex-row">
                <% if Current.user %>
                    <li>
                        <%= link_to "Log out", logout_path, method: :delete, class: "px-2 hover:bg-orange-400 rounded border border-transparent" %>
                    </li>
                <% else %>
                    <li class="px-2 py-2 hover:bg-orange-400 rounded border border-transparent"><a href="/sign_in">Log in</a></li>
                    <li class="px-2 hover:bg-orange-400 rounded border border-transparent"><a href="/sign_up">Sign Up</a></li>
                <% end %>
            </ul>
        </div>
    </nav>
</header>