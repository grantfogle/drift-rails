<div class="">


    <div class="container mx-auto max-w-[1200px] py-4 md:py-8 ">
        <%= render partial: "filters", locals: { current_user: @current_user } %>

        <section class="small-screen-view md:hidden" id="streams-cards">
            <%= render "cards", streams: @streams %>
        </section>

        <section class="hidden md:block" id="streams-table">
            <%= render "table", streams: @streams  %>
        </section>
    </div>
    <!-- TODO: dropdown should show lots of data like stream flows -->
    <!-- TODO: add pagination -->
</div>
