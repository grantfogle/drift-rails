<section id='map' style='width: 100vw; height: 80vh;'>
  <div id="tools-overlay" 
       class="absolute top-5 left-5 z-10">
    <%= render 'maps/components/search', locals: {flows: @flows } %>
  </div>

  <!-- TODO RENDER A POPUP FOR PDFs -->
  <!-- ALLOW FAVORITES TO BE DRAGGABLE -->
  <!-- POPUPS FOR LAYER ITEMS -->
  <!-- ALLOW USERS TO FAVORITE FLOW FROM POPUP -->
  <!-- STIMULUS REFLEX FOR FAVORITES, SO IT DOESNT ROUTE TO FAVORITES PAGE -->
  <!-- MORE DATA -->

  <% if session[:show_pdf] %>
    <div class="fixed inset-0 z-50 flex justify-center items-center bg-gray-900 bg-opacity-75">
        <h3>STUFFFFFF</h3>
        <!-- <iframe src="path_to_your_pdf" class="w-full h-full"></iframe> -->
    </div>
<% end %>


  <div id="popup-overlay" class="absolute top-5 right-5 z-10"></div>

  <!-- Map Legend -->
  <div id="layers-overlay" class="absolute bottom-5 left-5 z-10">
    <%= render 'maps/components/layers' %>
  </div>
</section>

<%= javascript_pack_tag 'mapSetup', defer: true %>
